# 🔧 Core dependencies
playwright==1.44.0        # Browser automation (Chromium + control)
requests==2.32.3          # HTTP API requests (Mail.tm, IP check)
termcolor==2.4.0          # Colored CLI banners/logs

# 🧪 Optional improvements
python-dotenv==1.0.1      # Load configuration from .env file
fake-useragent==1.5.1     # Generate random user agents if needed
PySocks==1.7.1            # SOCKS proxy support (unused now, optional)
